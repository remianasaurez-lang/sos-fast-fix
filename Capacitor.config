import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fixnowsos.app',
  appName: 'FixNow SOS',
  webDir: 'dist',
  bundledWebRuntime: false,
  // The app runs entirely from bundled local assets + localStorage,
  // so no server config / live-reload URL is needed for the offline build.
  server: {
    androidScheme: 'https'
  },
  android: {
    allowMixedContent: false
  }
};

export default config;
